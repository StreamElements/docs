---
title: "!closestore"
description: "Use the !closestore command in StreamElements chatbot to quickly disable all regular (non-SFX) items in your loyalty store."
platforms: [twitch]
keywords:
  - closestore command
  - disable store items
  - loyalty store
  - stream store
  - streamelements chatbot
  - store management
---

import ChatExample from '@components/ChatExample.astro';

The `!closestore` command allows streamers and moderators to quickly disable all **regular** (non-sound effect) items in the StreamElements loyalty store. This is useful for temporarily preventing viewers from redeeming items while making updates or during specific stream segments.

## Usage

To disable all regular store items, use the following command in chat:

```streamelements
!closestore
```

:::caution[Permissions]
By default, only users with **Moderator** permission level or higher can use this command.
:::

## Examples

<ChatExample messages={[
  { persona: 'moderator', message: '!closestore' },
  { persona: 'bot', message: '@Moderator, successfully updated 15 items.' }
]} />

## Configuration

*   The command's permission level can be adjusted under `Chatbot` -> `Chat Commands` -> `Default Commands`.

## Related Commands

*   [`!openstore`](/chatbot/commands/default/openstore): Re-enables all regular items in the loyalty store.
*   [`!disablesfx`](/chatbot/commands/default/disablesfx): Disables only sound effect (SFX) items in the loyalty store.
*   [`!enablesfx`](/chatbot/commands/default/enablesfx): Enables only sound effect (SFX) items in the loyalty store.
*   [`!redeem`](/chatbot/commands/default/redeem): Allows viewers to redeem store items.
