---
title: "!queue"
description: "Use the !queue command in StreamElements chatbot to manage viewer queues for activities like playing games with the streamer."
keywords:
  - queue command
  - viewer queue
  - play with viewers
  - manage queue
  - join queue
  - pick user
  - streamelements chatbot
platforms: [twitch]
access: everyone
summary: "Manage viewer queues for activities like playing with the streamer."
requires: Viewer Queue module
category: viewer-queue
---

import ChatExample from '@components/ChatExample.astro';

The `!queue` command suite helps manage a viewer queue, often used for activities like playing games with the streamer. It allows viewers to join, check their position, and moderators to manage the queue (open, close, pick, remove).

## Usage

The general syntax follows this pattern:

```streamelements
!queue <subcommand> [arguments...]
```

A subcommand is always required — typing `!queue` on its own does nothing.

:::caution[Permissions]
The management subcommands (`open`, `close`, `pause`, `unpause`, `pick`, `remove`) require **Moderator** permission level or higher. The viewer subcommands (`join`, `position`, `list`) are available to everyone.
:::

## Subcommands

Here's a breakdown of the available subcommands:

### `join`

Allows a viewer to enter the open queue.

:::note
You must type the full `!queue join` to enter the viewer queue. The standalone [`!join`](/chatbot/commands/default/join) command is unrelated — it enters an active raffle instead.
:::

*   **Syntax:** `!queue join`
*   **Example:**
    <ChatExample messages={[
      { persona: 'user', message: '!queue join' },
      { persona: 'bot', message: '@User, you have been added to the queue in position #5.' }
    ]} />

### `position`

Checks the command user's current position in the queue.

*   **Syntax:** `!queue position`
*   **Example:**
    <ChatExample messages={[
      { persona: 'user', message: '!queue position' },
      { persona: 'bot', message: '@User, you are currently position #3 in the queue.' }
    ]} />

### `list`

Lists who is currently waiting in the queue. The bot posts up to 5 names in chat, followed by a count of how many other viewers are also waiting.

*   **Syntax:** `!queue list`
*   **Example:**
    <ChatExample messages={[
      { persona: 'user', message: '!queue list' },
      { persona: 'bot', message: 'Currently waiting in the Viewer Games queue: UserOne, UserTwo, UserThree, UserFour, UserFive and 7 others.' }
    ]} />

### `open <title>` (Moderator+)

Opens the queue, allowing viewers to join. A title for the queue is **required**.

*   **Syntax:** `!queue open <title>`
*   **Example:**
    <ChatExample messages={[
      { persona: 'moderator', message: '!queue open Viewer Games' },
      { persona: 'bot', message: '@Moderator, The Viewer Games queue is now open! Viewers can type !queue join to enter.' }
    ]} />

### `close` (Moderator+)

Closes the queue, preventing new viewers from joining.

*   **Syntax:** `!queue close`
*   **Example:**
    <ChatExample messages={[
      { persona: 'moderator', message: '!queue close' },
      { persona: 'bot', message: '@Moderator, The queue is now closed.' }
    ]} />

### `pause` (Moderator+)

Temporarily stops viewers from joining the queue (similar to close, but implies temporary).

*   **Syntax:** `!queue pause`
*   **Example:**
    <ChatExample messages={[
      { persona: 'moderator', message: '!queue pause' },
      { persona: 'bot', message: '@Moderator, The queue is paused.' }
    ]} />

### `unpause` (Moderator+)

Resumes allowing viewers to join the paused queue.

*   **Syntax:** `!queue unpause`
*   **Example:**
    <ChatExample messages={[
      { persona: 'moderator', message: '!queue unpause' },
      { persona: 'bot', message: '@Moderator, The queue is no longer paused.' }
    ]} />

### `pick <random | username>` (Moderator+)

Selects a participant from the queue. You must specify either a username or the keyword `random` — there is no option to pick the next person in line.

*   **Syntax:** 
    *   `!queue pick random` (Selects randomly)
    *   `!queue pick <username>` (Selects a specific user)
*   **Example (random):**
    <ChatExample messages={[
      { persona: 'moderator', message: '!queue pick random' },
      { persona: 'bot', message: '@LuckyViewer has been randomly selected from the queue!' }
    ]} />
*   **Example (specific):**
    <ChatExample messages={[
      { persona: 'moderator', message: '!queue pick SpecificUser' },
      { persona: 'bot', message: '@SpecificUser has been picked from the queue!' }
    ]} />

### `remove <username>` (Moderator+)

Removes a specific user from the queue.

*   **Syntax:** `!queue remove <username>`
*   **Example:**
    <ChatExample messages={[
      { persona: 'moderator', message: '!queue remove ProblemUser' },
      { persona: 'bot', message: '@Moderator, ProblemUser has been removed from the queue.' }
    ]} />

## Configuration

*   The **Queue module** must be enabled in your StreamElements Dashboard under `Chatbot` -> `Modules`.
*   Restrictions on the queue, such as the maximum queue size and which viewers are eligible to join (for example followers or subscribers only), are configured in the module settings on the Dashboard.
*   Permission levels for subcommands can be adjusted under `Chatbot` -> `Chat Commands` -> `Default Commands`.

## Related Commands

*   [Viewer Queue Module](/chatbot/modules/viewerqueue): Enable the queue and configure its settings (size limits, messages, and behavior) in the StreamElements Dashboard.
