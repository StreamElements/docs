---
title: "!pause"
description: "Use the !pause command in StreamElements chatbot to temporarily stop the media request playback without clearing the queue."
keywords:
  - pause command
  - pause song
  - pause media request
  - song request pause
  - stream elements chatbot
platforms: [twitch]
access: moderator
requires: Song Requests
category: song-requests
---

import ChatExample from '@components/ChatExample.astro';

The `!pause` command temporarily stops the playback of the current item in the StreamElements media request queue. The queue itself remains intact, and playback can be resumed later.

## Usage

To pause the currently playing media:

```streamelements
!pause
```

## Examples

<ChatExample messages={[
  { persona: 'moderator', message: '!pause' },
  { persona: 'bot', message: '@Moderator, Media request playback paused!' }
]} />

## Configuration

*   The command's permission level can be adjusted under `Chatbot` -> `Chat Commands` -> `Default Commands`.

## Related Commands

*   [`!play`](/chatbot/commands/default/play): Resumes media playback after it has been paused.
*   [`!skip`](/chatbot/commands/default/skip): Skips the currently playing media item.
*   [`!volume`](/chatbot/commands/default/volume): Adjusts the volume of the media player.
*   [`!songrequest`](/chatbot/commands/default/songrequest): Adds a song/video to the queue.
*   [`!songqueue`](/chatbot/commands/default/songqueue): Provides a link to the media queue page.
