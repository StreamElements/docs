---
title: "!commands"
description: "Use the !commands command in StreamElements chatbot to display a link to the creator's public commands list page."
platforms: [twitch, youtube, kick]
keywords:
  - commands command
  - StreamElements
  - chatbot
  - custom commands
  - commands list
  - streamer commands
  - Twitch commands
  - YouTube commands
---

import ChatExample from '@components/ChatExample.astro';

The `!commands` command provides viewers with a link to the streamer's public StreamElements commands page. This page lists all enabled default and custom commands for the channel, making it easy for viewers to discover available interactions.

## Usage

To get the link to the commands page, simply type the command in chat:

```streamelements
!commands
```

## Examples

<ChatExample messages={[
  { persona: 'user', message: '!commands' },
  { persona: 'bot', message: 'You can find a list of all Commands here https://streamelements.com/styler/commands' }
]} />

## Configuration

*   This command is enabled by default.
*   The public commands page only shows commands that are currently enabled.
*   The list updates automatically based on your command settings in the StreamElements dashboard.

## Related Commands

*   [`!command`](/chatbot/commands/default/command): Used by moderators/streamers to manage custom commands directly via chat.
