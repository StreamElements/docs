// Legacy feed URL from the Docusaurus "Patch Notes" blog. Feed readers don't
// follow meta-refresh redirects, so keep serving the changelog feed here.
export { GET } from '../changelog/rss.xml.js';
